import React from 'react'
import CompFour from './CompFour'

const CompThree = props => {
    return (
        <div>
            <p>the data is {props.index}</p>
        </div>
    )
}

export default CompThree
